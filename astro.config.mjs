import { defineConfig } from "astro/config";

// @ts-check

/** @type {import('astro').AstroUserConfig} */

export default defineConfig({
  integrations: [],
  vite: {
    plugins: [],
  },
  outDir: "./dist/static",
});
