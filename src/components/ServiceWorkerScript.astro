---
const { src } = Astro.props;
---
{src ? <script define:vars={{src}}>
  globalThis.addEventListener('load', () => {
        globalThis.navigator.serviceWorker.register(src);
    });
</script> : null}